{
  "name": "factory-visualization",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm --filter front dev && pnpm --filter backend dev",
    "build": "pnpm --filter front build && pnpm --filter backend build",
    "docker:export": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/docker-export.ps1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ]
}
